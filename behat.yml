default:
    autoload:
        '': %paths.base%/test/behavioural/bootstrap
    suites:
        encryption.asymmetric:
            paths: [ %paths.base%/test/behavioural/encryption/asymmetric ]
            contexts:
                - AsymmetricEncryptionContext
